s_broadcast: N
is dissent worse case is 2 s_broacasts: 2N
bcast AUX: N

N=4

--------------------------------------------

> honest party inputs
> send corrupt alie message (VAL, ECHO, READY)
> deliver some index
> make progress

requires parties to handle all manner of messages out of order
--> forces UC spec to be closer to reality

CHECK: would ACast handle this?

--------------------------------------

the coin and token has a carrytokens returning on f2p
fMulticastToken also has CarryTokens on F2P

BenOr thresholds
N-t        One messages
	if (N+t)/2    One --> TwoD message
	ese 		  One --> Two message

N-t		   TwoX messages
	if t+1 		D-messages for v set x = v
	if (N+t)/2	D-messages decide v
	else xp = coin flip

-------------------------------------

S_VAL messages for both bits per round
	-- choose 0, 1 or both to S_VAL for each round
EST messages for both bits per round
	-- choose 0, 1 or both for EST for each round
AUX is treated the same way
